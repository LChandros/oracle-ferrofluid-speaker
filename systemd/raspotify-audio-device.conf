[Service]
# Configure librespot to output to loopback device
# This allows oracle_audio_manager to bridge audio to speakers
# while oracle_led_states_music.py captures for visualization
ExecStart=
ExecStart=/usr/bin/librespot --device hw:2,0
